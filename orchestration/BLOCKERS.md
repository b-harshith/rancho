# Blockers

| ID | Scope | Blocker | Evidence | Owner action | State |
|---|---|---|---|---|---|
| B-001 | Global | Workspace contains no Git metadata, so Git checkpoints are unavailable. | `git status` returned “not a git repository” on 2026-06-30. | None required; file-based checkpoint policy adopted. | mitigated |
| B-002 | Delhi NCR Stage 0 | Source mappings, approved NCR component boundary, and authoritative PIN provenance are not yet verified. | Registry fields intentionally null. | Mapping agent/orchestrator must produce evidence; human session action may later be needed. | open |
| B-003 | Bengaluru regression | Public artifacts remain mixed revisions. A hash-locked 309-H3 staging reconstruction now reconciles, but it packages accepted final inputs rather than reproducing source collection and retains countable TAM 0 versus direct TAM 316,676. | Baseline audit; `data/staging/bengaluru/current/run_manifest.json`; Wave 2 QA R1. | Method owner must approve/correct countable-TAM semantics before golden-fixture admission or public publish. | partially mitigated |
| B-004 | UDISE collection | Current worker automatically solves/logs CAPTCHA via EasyOCR, contrary to the required human-assisted-only policy. | `docs/multicity/SCRAPER_INVENTORY.md`, UDISE section. | Disable/remove OCR branch and prove dashboard-only human entry before any collection. | open |
| B-005 | 99acres collection | Candidate scripts contain embedded session-cookie defaults. | `docs/multicity/SCRAPER_INVENTORY.md`, 99acres section. | Source removed from the target localities pipeline by D-006; do not execute legacy copies. Rotate/revoke exposed sessions outside repo. | retired source / credential action open |
| B-006 | Deployment security | A literal Google API key fallback was exposed in `src/server.py`. The literal has been removed and R1 scans pass. | Baseline audit; `audits/wave2/QA_REPORT_R1.md`. | Owner must rotate/revoke the exposed key and review provider usage/restrictions; repository state cannot prove this. | code mitigated / owner action open |
| B-007 | Delhi NCR Google comparison | Boundary/PIN tooling is complete, but `GOOGLE_MAPS_API_KEY` is not available as a secure runtime environment variable; chat text is intentionally not copied into commands/files. | `data/cities/delhi_ncr/audits/geospatial/google_query_manifest.json`. | Owner must inject a rotated/restricted key into the runtime environment, then run five redacted center/viewport queries. | open |
| B-008 | Ezyschooling API mapping | Seven first-party NCR routes and 21/21 rendered samples are verified, but exact API `school_city` IDs/values and one-page API preflights remain unknown. | `docs/multicity/source_mappings/ezyschooling_delhi_ncr.md`. | Inspect first-party network calls in an authorized browser session; do not infer route slug as API value. | open |

Network, CAPTCHA, authentication, source-policy, and data-completeness blockers are recorded only when observed. A blocked source never authorizes fabricated replacement data.
