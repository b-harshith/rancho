# Bengaluru Status

Role: existing baseline/reference. Architecture/schema audit passed; numeric regression fixture and multi-city admission are blocked by mixed-revision H3/TAM artifacts (B-003). Existing raw/reference results must be preserved. Next gate: one-manifest atomic rebuild plus independent reconciliation.
